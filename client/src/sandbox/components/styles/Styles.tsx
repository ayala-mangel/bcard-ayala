import React, { CSSProperties } from "react";

const Styles = () => {
  const test: CSSProperties = {
    backgroundColor: "#8987",
    color: "white",
  };
  return <div style={test}>Styles</div>;
};

export default Styles;
